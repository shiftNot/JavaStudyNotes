刚刚 我们分别使用四种方式拷贝文件：
    1.普通字节流 单个字节拷贝文件    430820ms
    2.缓冲字节流 单个字节拷贝文件    1965ms
    3.普通字节流 字节数组拷贝文件    211ms
    4.缓冲字节流 字节数据拷贝文件    203ms
通过以上 可以明显发现 缓冲字节流读取数据的效率要远高于普通字节流
原因：就是在缓冲字节流中有一个缓冲数组，普通流没有，通过缓冲数组可以减少读写文件时硬盘和内存的交互次数
说明：如果你在使用普通字节流时，手动的加上一个缓冲数组 byte[8192]  就和使用缓冲流一样了
